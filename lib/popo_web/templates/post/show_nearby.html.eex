<h1>Listing Posts</h1>

<table>
  <thead>
    <tr>
      <th>Description</th>
      <th>Photo</th>
      <th>Location</th>

      <th></th>
    </tr>
  </thead>
  <tbody>
<%= for post <- @posts do %>
    <tr>
      <td><%= post.description %></td>

      <td>
        <span><%= link "Show", to: Routes.post_path(@conn, :show, post, type: "display") %></span>
        <span><%= link "Edit", to: Routes.post_path(@conn, :edit, post) %></span>
        <span><%= link "Delete", to: Routes.post_path(@conn, :delete, post), method: :delete, data: [confirm: "Are you sure?"] %></span>
      </td>
    </tr>
<% end %>
  </tbody>
</table>

<span><%= link "New Post", to: Routes.post_path(@conn, :new) %></span>
